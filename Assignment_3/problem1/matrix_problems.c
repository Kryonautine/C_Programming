#include <stdio.h>

int readMatrix() {
	int a;
	int columns, rows;

	scanf("%d", &columns);
	scanf("%d", &rows);

	int matrixa[columns][rows], matrixb[columns][rows];

	for 
}

int main(void) {
	int matrixa=readMatrix(matrixa), matrixb=readMatrix();
	readMatrix(matrixb);
}